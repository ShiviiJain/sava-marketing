# Vercel only needs source + lockfile and the marketing photography that is
# served directly from `apps/marketing/public/local`. Skip build/dev artifacts.

node_modules
.next
.turbo
.git
.env*.local
